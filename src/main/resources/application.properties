logging.level.com.mindpropel.springit=DEBUG

info.application.name=Springit
info.application.descritption=Reddit clone using Spring Boot 2
info.application.version-0.0.1

management.endpoints.web.exposure.include=*

management.endpoint.health.show-details=always

#h2
spring.h2.console.enabled=true
spring.datasource.name=springit

# sa | empty password
spring.datasource.username=
spring.datasource.password=

#JPA & Hibernate
#spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

#DDL mode. This is actually a shortcut for the "hibernate.hbm2ddl.auto"
#property. Defaults to "create-drop" when using an embedded database and no
#schema manager was detected. Otherwise, defaults to "none".
# create | create-drop | none | update | validate
#spring.jpa.hibernate.ddl-auto=


# mysql settings
#spring.jpa.hibernate.ddl-auto=none
#spring.datasource.url=jdbc:mysql://localhost:3306/springit?useSSL=false&allowPublicKeyRetrieval=true
#spring.datasource.username=springit
#spring.datasource.password=F00b@r!23
#
## This allows us to have intialization data in the DB - in Spring Boot 2 for non-inmemory DB's
#spring.datasource.initialization-mode=always

